import { CheckWorkingHoursPipe } from './check-working-hours.pipe';

describe('CheckWorkingHoursPipe', () => {
  it('create an instance', () => {
    const pipe = new CheckWorkingHoursPipe();
    expect(pipe).toBeTruthy();
  });
});
